import { ParentMessagesPage } from "@/features/parent/ParentPages"; export default async function Page({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <ParentMessagesPage conversationId={id} />; }
